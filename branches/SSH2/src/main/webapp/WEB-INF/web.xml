<?xml version="1.0" encoding="GBK"?>
<web-app xmlns="http://java.sun.com/xml/ns/javaee"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://java.sun.com/xml/ns/javaee
	http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd" version="3.0">

	<!-- 配置Spring配置文件的位置 -->
	<context-param>
		<param-name>contextConfigLocation</param-name>
		<param-value>/WEB-INF/applicationContext.xml,
			/WEB-INF/daoContext.xml,/WEB-INF/boContext.xml</param-value>
	</context-param>
	<!-- 使用ContextLoaderListener初始化Spring容器 -->
	<listener>
		<listener-class>org.springframework.web.context.ContextLoaderListener
		</listener-class>
	</listener>

	<!-- 定义Struts2框架的核心Filter -->
    <filter>
        <filter-name>struts2Prepare</filter-name>
        <filter-class>
            org.apache.struts2.dispatcher.ng.filter.StrutsPrepareFilter</filter-class>
    </filter>
	<!-- 定义SiteMesh框架的核心Filter -->
    <filter>
        <filter-name>sitemesh</filter-name>
        <filter-class>com.opensymphony.sitemesh.webapp.SiteMeshFilter</filter-class>
    </filter>
    <!-- 定义Struts2框架的核心的Filter -->
    <filter>
        <filter-name>struts2Execute</filter-name>
        <filter-class>
            org.apache.struts2.dispatcher.ng.filter.StrutsExecuteFilter</filter-class>
    </filter>
	
	<!-- 第1个过滤器：让ActionContextCleanUp的核心Filter拦截所有请求 -->
	<filter-mapping>
		<filter-name>struts2Prepare</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	
	<!-- 第2个过滤器：让SiteMesh框架的核心Filter过滤所有用户请求 -->
	<filter-mapping>
		<filter-name>sitemesh</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	
	<!-- 第3个过滤器：让Struts 2的核心Filter拦截所有请求 -->
	<filter-mapping>
		<filter-name>struts2Execute</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>

	<!-- 定义Web应用的首页 -->
	<welcome-file-list>
		<welcome-file>index.jsp</welcome-file>
	</welcome-file-list>
</web-app>

